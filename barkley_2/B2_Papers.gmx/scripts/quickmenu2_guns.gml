if (argument[0] == "generate") {
    border.generate(0, 100, 100);
} else if (argument[0] == "draw") {
    border.draw()
    if(global.quickmenuActive == 0){
        //Hide animation
    } else {
        //Show animation
    }
}
