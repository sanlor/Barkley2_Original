// event_chu_elevator_readout

if (argument0 == SCRIPT_START) {
    var elevator_readout = id;
    var event = scr_event_create_background();
    
    with (event) {

    }  
    scr_event_advance(event);
}