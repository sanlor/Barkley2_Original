/// Execute(event script)
/// Builds an action to execute another event script (will end the current one).

var action = ds_list_create();
ds_list_add(action, scr_event_action_execute_event_script);
ds_list_add(action, argument0); // script name
ds_list_add(_event_list, action); // add the action to the list

return ds_list_size(_event_list) - 1;