/// Snippet(event)
/// Builds an action to execute an event snippet.

var action = ds_list_create();
ds_list_add(action, scr_event_action_event_snippet);
ds_list_add(action, argument0); // event
ds_list_add(_event_list, action); // add the action to the list

return ds_list_size(_event_list) - 1;