/// scr_event_build_door_unlock(door name)
/// Builds an action to unlock a door

var action = ds_list_create();
ds_list_add(action, scr_event_action_door_unlock);
ds_list_add(action, argument0); // the door to unlock
ds_list_add(_event_list, action); // add the action to the list

return ds_list_size(_event_list) - 1;