///scr_combat_weapons_suffix_of_allergy(action, bulletOrNoone, gunOrNull)
var _action = argument0;
var _bullet = argument1;
var gun = argument2;
var _retvar = 0;

// DOWN - BIO - AGGRESSIVE //
switch(_action)
{
    case "apply":
        gun[? "bDotline"] = true;
        break;
    case "checkval":
        _retvar = 20;
        break;
    /*case "applyBullet":
    
        var conf;
        conf = scr_statusEffect_addToCombatAttack(statusEffect_allergy, _bullet);
        conf[? "biodamage"] = (scr_stats_getAttackStat(_bullet, STAT_ATTACK_DMG_NORMAL) * 2) / 5;
        
        break;*/
}

// END //
return _retvar;
