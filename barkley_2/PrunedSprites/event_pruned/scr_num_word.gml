// scr_num_word()

/*
var num = string(argument0);
var digits = string_length(num);
var i = 0;
var cur = 0;
var word = "";
var to_add = "";

for (i = digits; i == 1; i -= 1)
{
    cur = string_char_at(num, i);
    if (i == 1)
    {
        if (cur == 0) || (cur == 1) || (cur == 2)
        {
            if (string_char_at(num, i - 1) == 1)
            {
                i -= 1;
                if (cur == 0) { to_add = "ten" }
                else if (cur == 1) { to_add = "eleven" }
                else if (cur == 2) { to_add = "twelve"
                word = string_insert(
            }
        }
    }
}
*/

//else { return string(argument0) }
