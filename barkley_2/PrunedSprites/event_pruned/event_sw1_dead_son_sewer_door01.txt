if (argument0 == SCRIPT_START) {

}
else if (argument0 == SCRIPT_STEP) {
}
else if (argument0 == SCRIPT_INTERACT) {
    scr_quest_set_state("deadSonSewer", 1);
    var event = scr_event_create_main();
    with (event) {
        scr_event_build_dialogue("Matthias", s_port_matthias, "(I don't think I should go back into the sewers, I've gotta get Matthias to Firas.)");
        scr_event_build_move(o_cts_hoopz, 0, 16);
        scr_event_build_wait_for_actions();
        scr_event_build_quest_state("deadSonSewer", 0);
    }
    scr_event_advance(event);
}
