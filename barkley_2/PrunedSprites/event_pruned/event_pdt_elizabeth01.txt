// Crazy Robinsons // Elizabeth //
// Initialize //
/*if (argument0 == SCRIPT_START) {

    // or (scr_quest_get_state("pdt_yellowkid_snitched") >= 1) // Why was this in here I cant remember, doesnt seem right //
    // Can occur only once //
    if (scr_quest_get_state("pdt_zorro") == 1)  or (scr_quest_get_state("pdt_zorro_fritz") >= 2) or (scr_time_get() <= 1) or (scr_time_get() >= 10) scr_event_interactive_deactivate();

} 

// Step //
if (argument0 == SCRIPT_STEP) {

}

// Interact //
else if (argument0 == SCRIPT_INTERACT) {

        // Variables //
        var event = scr_event_create_main();
        scr_entity_look(id, EAST);
        // Main event //
        with (event) {
                
                scr_event_build_dialogue("Elizabeth Swiss Robinson", "s_port_sfr_elizabeth", "Jack is the third oldest son. He is 11 years old. He is bold, vivacious and thoughtless, but he is also the quickest.");
                }
        scr_event_advance(event);
}
