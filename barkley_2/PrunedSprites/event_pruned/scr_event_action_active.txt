/// scr_event_action_active(list)
/// object
/// interact_snabled
/// collision_enabled
/// shadow_enabled

with (ds_list_find_value(argument0, 1))
{
    if (ds_list_find_value(argument0, 2) == 0) scr_event_interactive_deactivate();
    else scr_event_interactive_activate();
}

scr_event_advance(id);